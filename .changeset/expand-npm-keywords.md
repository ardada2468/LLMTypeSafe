---
'@ts-dspy/anthropic': patch
'@ts-dspy/gemini': patch
'@ts-dspy/openai': patch
'@ts-dspy/core': patch
---

Expand npm keywords so the packages surface for the searches people actually
run — `zod`, `json-schema`, `structured-outputs`, `validation`, `type-safe`,
`tool-calling`, and per-provider terms like `gpt`, `claude`, and `gemini-api`.
